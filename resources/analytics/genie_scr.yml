# Genie Room for SCR exploration — placeholder
resources:
  genie_rooms:
    genie_scr:
      display_name: "SCR Explorer — R.18 Compliance"
      description: "Genie room for natural-language queries over SCR 3040/3050 data"
      warehouse_id: ${var.warehouse_id}
      table_identifiers:
        - "${var.catalog}.${var.schema_gold}.posicao_mensal_3040"
        - "${var.catalog}.${var.schema_gold}.posicao_diaria_3050"
        - "${var.catalog}.${var.schema_gold}.reconciliacao_cosif"
        - "${var.catalog}.${var.schema_gold}.reconciliacao_3040_3050"
        - "${var.catalog}.${var.schema_quality}.quality_scorecard"
