# Gold reconciliation pipeline — placeholder
resources:
  pipelines:
    gold_reconciliation:
      name: "r18-gold-reconciliation-${bundle.target}"
      catalog: "${var.catalog}"
      schema: "${var.schema_gold}"
      libraries:
        - notebook:
            path: ../../pipelines/gold/transformations/criticas_summary.py
        - notebook:
            path: ../../pipelines/gold/transformations/posicao_mensal.py
        - notebook:
            path: ../../pipelines/gold/transformations/quality_scorecard.py
        - notebook:
            path: ../../pipelines/gold/transformations/reconciliacao_3040_3050.py
